plugins {
    id("wave-sample-app")
    id("com.google.devtools.ksp")
    alias(libs.plugins.compose.compiler)
}

android {
    namespace = "org.wave.imageeditor.app"

    defaultConfig {
        applicationId = "org.wave.imageeditor.app"
    }
}

dependencies {
    implementation(project(":image-editor"))

    implementation(libs.glide.glide)
    ksp(libs.glide.ksp)
}
