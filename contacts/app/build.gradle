plugins {
    id 'wave-sample-app'
    alias(libs.plugins.compose.compiler)
}

android {
    namespace 'org.wave.contactstest'

    defaultConfig {
        applicationId "org.wave.contactstest"
    }
}

dependencies {
    implementation project(':contacts')
}